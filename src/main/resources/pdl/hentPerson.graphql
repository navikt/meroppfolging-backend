query($ident: ID!) {
    hentPerson(ident: $ident) {
        foedselsdato {
            foedselsdato
        }
        navn(historikk: false) {
            fornavn
            mellomnavn
            etternavn
        }
    }
}

{
  "interactiveLogin": true,
  "httpServer": "NettyWrapper",
  "tokenCallbacks": [
    {
      "issuerId": "tokenx",
      "tokenExpiry": 3600,
      "requestMappings": [
        {
          "requestParam": "code",
          "match": "tokenx",
          "claims": {
            "acr": "${acr}",
            "pid": "${pid}",
            "sub": "${pid}",
            "aud": "${aud}",
            "client_id": "${client_id}"
          }
        }
      ]
    }
  ]
}
